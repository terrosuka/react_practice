import styled from "styled-components"

export default function ForgetPassword() {
  return (
    <div>
      <main>
        <SCText>パスワード再設定</SCText>
      </main>
    </div>
  )
}

const SCText = styled.p`
  color: red;
`
